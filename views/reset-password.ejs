<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="shortcut icon" href="/logo.png" type="image/png" />
    <title>المعلم التكنولوجي - إعادة تعيين كلمة المرور</title>
    <style>
      * {
        margin: 0;
        padding: 0;
        box-sizing: border-box;
      }

      html {
        background-color: #eee;
        font-family: system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI",
          Roboto, Oxygen, Ubuntu, Cantarell, "Open Sans", "Helvetica Neue",
          sans-serif;
      }

      body {
        display: grid;
        place-content: center;
        height: 80vh;
      }

      .container {
        width: 150px;
        max-width: 150px;
        display: flex;
        flex-direction: column;
        justify-content: center;
        align-items: center;
        gap: 15px;
      }

      .logo {
        width: 150px;
        object-fit: contain;
      }

      .form {
        display: flex;
        flex-direction: column;
        justify-content: center;
        align-items: center;
        gap: 15px;
      }

      #password1,
      #password2 {
        outline: none;
        border: none;
        padding: 10px 20px;
        border-radius: 3px;
        text-align: center;
      }

      #password1::placeholder,
      #password2::placeholder {
        text-align: center;
        font-weight: 600;
      }

      #password1:focus,
      #password2:focus {
        border: 1.3px solid #1ae5da;
      }

      .error {
        display: none;
        font-size: 12px;
        text-align: center;
        color: #d00;
      }

      .form-btn {
        border: none;
        outline: none;
        width: 100%;
        height: 35px;
        color: #fff;
        background-color: #1ae5da;
        font-size: 17px;
        font-weight: 600;
        transition-duration: 176ms;
        border-radius: 3px;
        cursor: pointer;
      }

      .form-btn:active {
        transform: scale(0.96);
      }
    </style>
  </head>
  <body>
    <div class="container">
      <img class="logo" src="/logo.png" alt="logo" />

      <form method="post" class="form">
        <input
          id="password1"
          name="password1"
          type="password"
          placeholder="كلمة المرور الجديدة"
          autofocus
        />

        <input
          id="password2"
          name="password2"
          type="password"
          placeholder="تأكيد كلمة المرور"
        />

        <p id="error" class="error">حصل خطأ عند إعادة تعيين كلمة المرور</p>

        <button type="submit" id="form-btn" class="form-btn">
          إعادة تعيين
        </button>
      </form>
    </div>
  </body>
</html>
